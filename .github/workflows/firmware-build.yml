name: Firmware Build

on:
  push:
    branches:
      - "**"
    paths:
      - "firmware/**"
      - "tests/**"
      - ".github/workflows/firmware-build.yml"
      - ".github/workflows/firmware-release.yml"
  pull_request:
    paths:
      - "firmware/**"
      - "tests/**"
      - ".github/workflows/firmware-build.yml"
      - ".github/workflows/firmware-release.yml"
  workflow_dispatch:

permissions:
  contents: read

jobs:
  host-tests:
    name: Host tests (policy truth table)
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repo
        uses: actions/checkout@v4
      - name: Run host tests
        run: node --test tests/*.test.mjs

  build:
    name: Build ESP32 firmware
    runs-on: ubuntu-latest

    steps:
      - name: Checkout repo
        uses: actions/checkout@v4

      - name: Build firmware
        uses: espressif/esp-idf-ci-action@v1
        with:
          esp_idf_version: v5.3.1
          target: esp32
          path: .
          command: tools/idf-build.sh firmware esp32 build

      - name: Upload firmware build outputs
        uses: actions/upload-artifact@v4
        with:
          name: dragonvent-firmware-${{ github.sha }}
          if-no-files-found: error
          retention-days: 14
          path: |
            firmware/build/dragonvent.bin
            firmware/build/dragonvent.elf
            firmware/build/dragonvent.map
            firmware/build/bootloader/bootloader.bin
            firmware/build/partition_table/partition-table.bin
            firmware/build/ota_data_initial.bin
            firmware/build/flasher_args.json
            firmware/build/flash_args
